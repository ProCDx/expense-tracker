# Expense Tracker

A full-stack web app for tracking personal expenses — add, categorize,
and monitor spending with budget warnings.

## Tech Stack
- **Backend:** Java, Spring Boot
- **Frontend:** HTML, CSS, JavaScript
- **Build tool:** Maven

## Features
- [Add/edit/delete expenses — adjust to what your app actually does]
- Budget tracking with warnings when nearing limits
- [Any other real feature — categories, charts, etc.]

## Run Locally

**Backend:**
```bash
cd backend
mvn spring-boot:run
```

**Frontend:**
Open `index.html` in your browser (or serve it via a local server if it calls the backend API).
